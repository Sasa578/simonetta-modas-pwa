const { Pool } = require('pg'); const pool = new Pool({ user: 'postgres', host: 'localhost', database: 'P_Simonetta_modas', password: 'admin', port: 5432 }); const run = async () => { try { await pool.query('ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS nombre_completo VARCHAR(150); ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS carnet_identidad VARCHAR(50); ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS telefono VARCHAR(50); ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS fecha_registro TIMESTAMP DEFAULT CURRENT_TIMESTAMP;'); console.log('Migration done'); process.exit(0); } catch(e) { console.error(e); process.exit(1); } }; run();  
